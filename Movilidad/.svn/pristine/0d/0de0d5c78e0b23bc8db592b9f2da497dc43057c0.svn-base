package co.gov.movilidadbogota.sipa.ctrl;

import co.gov.movilidadbogota.sipa.data.biz.comp.ReincidenciaRepo;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Controller;

/**
 * Se encarga de administrar las reincidencias en comparendos que cuenten con recurso.
 *
 * @autor maria.rodriguez
 */
@Controller
public class ReincidenciaController extends BaseController {

    public static final String CONTROLLER_PATH = "/reincidencia";
    public static final String CONTROLLER_PATH_CONSULTA = CONTROLLER_PATH + "/consulta";
    public static final String CONTROLLER_PATH_RECIBIR_RECURSO = CONTROLLER_PATH + "/recibir-recurso";
    public static final String CONTROLLER_PATH_REPARTO = CONTROLLER_PATH + "/reparto";


    @Autowired
    ReincidenciaRepo reincidenciaRepo;


}
