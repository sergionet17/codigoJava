angular.module('forest5').factory('tareasAsignar', function($resource,$http,CONFIG){
  var variables = {variables : '@variables'};
  //CONFIG.url+'/'+CONFIG.contexto+'/'+'task/'+tarea.id+'/assignee';
  return {
    post: function(idTarea, variables){
          alert('factory');
          alert(idTarea);
          alert(variables);
          //return 'x';
          //$http.post(CONFIG.url+'/'+CONFIG.contexto+'/task/'+idTarea+'/assignee',variables,{}).then(function(data){console.log('Tarea asignada')},function(error){console.log('Fallo asignando tarea')});
    }
  }
});
